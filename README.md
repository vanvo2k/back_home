# TeeAMZ Backend

## Install packages
- Install Node Canvas:

`https://github.com/Automattic/node-canvas#installation`

- Install node module:

`yarn || npm install`

## Development
`yarn _production`

`yarn start`